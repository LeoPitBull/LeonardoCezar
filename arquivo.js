// Obtendo o botão pelo ID
const botao = document.getElementById("meuBotao");

// Adicionando um ouvinte de evento de clique ao botão
botao.addEventListener("click", function() {
  // Mensagem que será exibida após o clique
  const mensagem = "QUE BOTADA BRABA!.";

  // Exibindo a mensagem em um alerta
  alert(mensagem);
});
